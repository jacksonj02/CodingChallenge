const input = require("readline-sync");
//areaOfTriangle

const base = input.question("What is the base of the triangle?\n")
const height = input.question("What is the height of the triangle\n")
console.log((base * height) / 2);
// returns the min and max number of an array
// function minMax(arr) {
//   return [Math.min(...arr), Math.max(...arr)];
// }
// const num = input.question("Input numbers")
// num = array
// console.log(num.sort)

// const yet = input.numbersse(fd)